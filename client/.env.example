# API base URL (no trailing slash). Set this in Vercel for production.
# Production (Render):
VITE_API_URL=https://ecommerce2-48a1.onrender.com/api
# Local dev (omit or leave empty to use default http://localhost:3001/api):
# VITE_API_URL=
